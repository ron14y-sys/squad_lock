# Copy to .env and fill in real values locally. Never commit .env — see
# docs/spec.md §10 ("secrets never enter the repo") and AGENTS.md.

# Postgres connection string (spec §6.3). Locally, e.g.:
# postgresql://squadlock:squadlock_dev@localhost:5432/squadlock_dev
DATABASE_URL=
