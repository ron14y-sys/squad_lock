# Design Decisions — closing the open questions

D1–D12 were resolved in a single review session before implementation began. **D13–D20 come from a later review of the external services**, where each API and scope was checked in our own Google Cloud Console and against Google's current documentation rather than from memory. Each decision is written up in full in the [spec](../spec.md); this page is the index and the one-line reason.

The spec is the source of truth. If this page and the spec disagree, the spec is right.

**Being closed is not the same as being right forever** — but it does mean the question is not reopened without new evidence. The genuinely undecided items have their own section below, and they are undecided on purpose.

| #   | Decision                                                                                                         | Why                                                                                                                                                                                                                                                             | Spec        |
| --- | ---------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------- |
| D1  | **Leximin** over normalized burden, not plain minimax                                                            | Plain minimax is degenerate: two candidates with the same worst-off participant are equivalent to it, so ties get broken by star rating and the fairness silently disappears                                                                                    | §5.4        |
| D2  | Travel tolerance stored in **kilometres**, presented as a labelled slider                                        | A 1–5 scale is an invisible mapping table nobody remembers by week 6; kilometres can be unit-tested and printed back to the user                                                                                                                                | §5.1, §5.4  |
| D3  | Above the gate, fairness is **advice, not binding**                                                              | Every candidate that survives the gate is valid, and choosing among valid options is the agent's job. The gate is where fairness is enforced                                                                                                                    | §5.4, §4.1f |
| D4  | Fairness **gates**; the shortlist is filled from **two parallel ranked lists**                                   | There is no exchange rate between kilometres and stars, and a weighted sum lets a great rating buy its way past unfairness — the thing the fairness layer exists to prevent                                                                                     | §5.4        |
| D5  | **Adaptive polling** for the feed, not a live connection                                                         | A held-open connection bills by duration, needs a pub/sub service to fan out, and brings reconnection work — for a surface where seconds of staleness are acceptable by design. The initiator's live progress already comes free from her own streaming request | §5.6        |
| D6  | **One query per neighbourhood**, cached by rounded coordinates                                                   | A local search API caps results per query, so one wide query means worse coverage per participant. Keying the cache on the neighbourhood makes it shared across meetings and users — and the rounding already exists, for privacy                               | §5.4, §6.3  |
| D7  | **Track A** owns the Context Resolver end to end                                                                 | It is the twin of the Constraint Updater — same model, same shape, same validation conventions. One person writes those conventions once                                                                                                                        | plan.md     |
| D8  | Shortlist of **20–24**                                                                                           | Two parallel lists need room for both                                                                                                                                                                                                                           | §5.4        |
| D9  | A rejection **always** triggers a new run; previous ranks stay as candidates; the rejected option may not return | Answering from a pre-computed rank cannot address an objection it never saw, and reads as not having listened — while the visible response to the objection _is_ the product                                                                                    | §13, §9     |
| D10 | Conflict = same day, **under 4 hours apart**, with two escape hatches                                            | A meeting has no duration, so literal overlap is not computable. And because a conflict triggers a cancellation, a false positive destroys someone else's evening — so it needs a way out                                                                       | §5.7        |
| D11 | **Weather is out of scope**                                                                                      | Forecasts for a meeting proposed days ahead are unreliable and are never refreshed on the night, so the data would be wrong precisely when it mattered                                                                                                          | §11         |
| D12 | Amendments are **batched in a ~90-second window**, closed by the feed poll                                       | Amendments cluster — several people open the same proposal within two minutes — so firing immediately runs the match twice and replaces a proposal before anyone finished reading it                                                                            | §3.2        |
| D13 | **Calendar is an availability input only**, on the non-sensitive `calendar.freebusy` scope                       | Verified in our own Cloud Console: `calendar.freebusy` exposes availability, not event content, and is **non-sensitive** — so the app publishes In production with no verification. `calendar.readonly` is not needed and is not requested                      | §5.2, §6.3  |
| D14 | **Calendar event creation is deferred**, and is not an MVP dependency                                            | Writing an event needs `calendar.events`, checked and classified **sensitive**. The MVP flow ends at an approved proposal, so paying a verification cost for an output nobody asked for would be backwards                                                      | §3, §5.2    |
| D15 | **No Gmail API and no Gmail scope.** Notifications go through a transactional email provider                     | The notification has no human sender and we never read user mail, so `gmail.send` — a sensitive scope that mails from a participant's own address — is the wrong mechanism twice over. An application-owned sender does it better and cheaper                   | §5.5, §6.3  |
| D16 | **Notification is a side effect, never part of meeting state**                                                   | If a failed send could cancel a meeting, an email outage would become a silent scheduling bug in state people had already agreed to. An approved proposal is valid whether or not the email landed; the failure is retried and recorded                         | §5.5        |
| D17 | **No Google Maps JavaScript API and no Routes API for MVP**                                                      | Nothing in v1 draws a map, and the burden model is straight-line distance × detour factor. Routes would buy real travel time, which v1 does not claim to compute — so it is an upgrade path, not a dependency                                                   | §4.4, §5.4  |
| D18 | **Places is billed per SKU, by the fields requested** — so the field mask is an architectural decision           | "Places is free" is false: it is free within per-SKU thresholds, and a single Enterprise field pulls a whole request into the 1,000/month tier. Cost follows the field mask, not the call count, so the masks are written down explicitly and `*` never ships   | §6.3        |
| D19 | **The candidate pool is provider-ranked, not exhaustive**                                                        | Text Search caps at 60 results per query and does not guarantee identical results for identical requests. We query per neighbourhood to reduce that bias, but "we search all venues in the area" is a claim the data does not support and we do not make it     | §5.4, §6.3  |
| D20 | **One Google Cloud project for the whole application**                                                           | Calendar (per-user OAuth) and Places (a project API key) are two APIs on one project. Per-user access comes from OAuth consent, not from separate projects — and a later Maps or Routes addition would be one more enabled API, not new infrastructure          | §6.3        |
| —   | **No comparative cost line** in a proposal                                                                       | Naming a cost manufactures a grievance that did not exist. Naming a _constraint_ is not the same as naming a _comparison_                                                                                                                                       | §5.6        |

## Deliberately still open — do not write these up as decided

Everything above is closed. The list below is not, and an item here is an **open decision or an implementation task**, never a design this page forgot to record. If a document, a diagram, or a report paragraph states an answer to one of these, that document is wrong.

| Open item                                            | What is settled                                                                                | What is not                                                                | Spec   |
| ---------------------------------------------------- | ---------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------- | ------ |
| Exact transactional email provider                   | Backend API key, application-owned sender, no Gmail OAuth                                      | Whether it is Resend or an equivalent; the free tier is unverified         | §13.8  |
| Sender address and domain setup                      | It is application-owned, never a participant's mailbox                                         | Which domain, DNS verification, the `From`/`Reply-To` shape                | §13.9  |
| Places field mask for the **broad search**           | It is explicit, minimal, and never `*`                                                         | The exact list of fields                                                   | §13.4  |
| Places field mask for **shortlist details**          | Expensive fields belong here, not in broad discovery                                           | The exact list of fields                                                   | §13.5  |
| Is `rating` required, or only a ranking signal?      | It is an **Enterprise** field, so the answer has a price                                       | Whether the funnel needs it at all                                         | §13.6  |
| Are opening hours a hard constraint or a preference? | `regularOpeningHours` is **Enterprise**; a hard constraint must be checked for every candidate | Which of the two it is — §5.4 currently gates on it, and that is not final | §13.7  |
| Database technology and schema                       | PostgreSQL is the intent; §6.2 lists indicative entities                                       | The concrete choice, hosting, migrations, and the final schema             | §13.11 |
| Authentication implementation                        | Sign-in with Google, `calendar.freebusy` consent                                               | Library, session model, token storage and refresh                          | §13.12 |
| Email retry and failure recording                    | A failed send never invalidates an approved proposal (D16)                                     | Retry policy, backoff, dead-letter, how a failure surfaces                 | §13.10 |
| Places caching strategy                              | Two tiers, keyed by neighbourhood (D6)                                                         | Cache keys, TTLs, invalidation, and the store                              | §13.13 |
| Rate-limit and retry strategy for external APIs      | Nothing                                                                                        | All of it, across Places, Calendar and email                               | §13.14 |

Items 4–7 of [spec §13](../spec.md) are one cluster, not four questions: **which venue attributes are required for correctness** determines the field masks, and the field masks determine the cost. Resolve them together, before the Places integration is written.

## The two that shaped everything else

**The principle.** Code narrows only on what is true or false. The model decides among what is valid. This is what answers both "should the agent be bound by the fairness ranking?" and "should the model compute the search area?" with one rule instead of two unrelated ones.

**The invariant.** The model may only widen retrieval; it may narrow only in scoring. Narrowing at retrieval is irreversible — a venue never fetched cannot be recovered by any later stage, including the rejection loop. Narrowing at scoring is recoverable. So the worst case of a bad model output is one extra API query, never a starved candidate set.

Both are recorded as binding decisions in [spec §4.1f and §4.1g](../spec.md), with the architecture decision they produced in [§4.3](../spec.md).

**And the external services were chosen to serve the same principle**, which is why D13–D20 are not a separate concern from D1–D12 ([spec §4.4](../spec.md)):

- **Calendar supplies availability** — a fact, and an input only. Nothing is written back.
- **Places supplies candidate venues** — a pool, not a ranking of the decision.
- **Code performs the deterministic work**: hard-constraint filtering, distance, burden, leximin.
- **The LLM chooses among valid candidates.** It does not compute a distance and cannot override a hard constraint.
- **Email is a notification mechanism, sitting outside meeting state entirely.**
- **Maps and routing are excluded** because neither moves a decision to where it belongs — one draws, and the other improves a number that already lives in the deterministic column.
